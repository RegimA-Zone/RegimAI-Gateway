<!--
    Dermatology AI Agents Policy
    Specialized policies for skincare and clinical dermatology agents

    This policy implements:
    1. Agent-specific authentication and authorization
    2. Professional credential verification for clinical agents
    3. Conversation context management
    4. Medical safety guardrails
    5. Response quality enforcement
-->
<policies>
    <inbound>
        <base />

        <!-- Validate Agent API authentication -->
        <validate-azure-ad-token tenant-id="{{tenant-id}}" failed-validation-httpcode="401" failed-validation-error-message="Unauthorized: Invalid agent access token">
            <audiences>
                <audience>{{agents-api-audience}}</audience>
            </audiences>
        </validate-azure-ad-token>

        <!-- Extract agent type from operation -->
        <set-variable name="agentType" value="@{
            var opId = context.Operation.Id;
            if (opId.StartsWith("skincare-consultant")) return "skincare_consultant";
            if (opId.StartsWith("clinical-assistant")) return "clinical_assistant";
            if (opId.StartsWith("product-advisor")) return "product_advisor";
            return "unknown";
        }" />

        <set-variable name="requestBody" value="@(context.Request.Body.As<JObject>(preserveContent: true))" />

        <!-- Clinical Assistant: Require professional credentials -->
        <choose>
            <when condition="@((string)context.Variables["agentType"] == "clinical_assistant")">
                <!-- Verify healthcare professional credentials -->
                <set-variable name="professionalCredentials" value="@{
                    var token = context.Request.Headers.GetValueOrDefault("X-Professional-Credentials", "");
                    // In production, validate against credential verification service
                    return !string.IsNullOrEmpty(token);
                }" />

                <choose>
                    <when condition="@(!(bool)context.Variables["professionalCredentials"])">
                        <return-response>
                            <set-status code="403" reason="Forbidden" />
                            <set-body>@{
                                return new JObject(
                                    new JProperty("error", "credentials_required"),
                                    new JProperty("message", "Clinical assistant requires verified healthcare professional credentials"),
                                    new JProperty("instruction", "Include X-Professional-Credentials header with valid credentials token")
                                ).ToString();
                            }</set-body>
                        </return-response>
                    </when>
                </choose>

                <!-- Add clinical context to request -->
                <set-header name="X-Agent-Mode" exists-action="override">
                    <value>clinical</value>
                </set-header>
            </when>
        </choose>

        <!-- Validate session management -->
        <choose>
            <when condition="@(context.Operation.Id.Contains("chat"))">
                <set-variable name="sessionId" value="@{
                    var body = (JObject)context.Variables["requestBody"];
                    return body?["session_id"]?.ToString() ?? "";
                }" />

                <choose>
                    <when condition="@(string.IsNullOrEmpty((string)context.Variables["sessionId"]))">
                        <!-- Auto-generate session ID if not provided -->
                        <set-variable name="sessionId" value="@(Guid.NewGuid().ToString())" />
                    </when>
                </choose>

                <set-header name="X-Session-Id" exists-action="override">
                    <value>@((string)context.Variables["sessionId"])</value>
                </set-header>
            </when>
        </choose>

        <!-- Content safety validation for user messages -->
        <set-variable name="userMessage" value="@{
            var body = (JObject)context.Variables["requestBody"];
            return body?["message"]?.ToString() ?? "";
        }" />

        <!-- Block requests for prescription-only advice without clinical credentials -->
        <choose>
            <when condition="@{
                var msg = ((string)context.Variables["userMessage"]).ToLower();
                var agentType = (string)context.Variables["agentType"];
                var isPrescriptionQuery = msg.Contains("prescription") || msg.Contains("dosage") ||
                                         msg.Contains("medication") || msg.Contains("drug");
                return isPrescriptionQuery && agentType != "clinical_assistant";
            }">
                <return-response>
                    <set-status code="400" reason="Bad Request" />
                    <set-body>@{
                        return new JObject(
                            new JProperty("error", "scope_limitation"),
                            new JProperty("message", "Prescription medication advice requires the clinical assistant agent"),
                            new JProperty("recommendation", "Please consult a healthcare professional or use the clinical assistant with proper credentials")
                        ).ToString();
                    }</set-body>
                </return-response>
            </when>
        </choose>

        <!-- Set agent-specific system prompts -->
        <set-variable name="systemPrompt" value="@{
            var agentType = (string)context.Variables["agentType"];

            switch (agentType) {
                case "skincare_consultant":
                    return @"You are a RégimA skincare consultant AI assistant, representing the RégimA cosmeceutical brand (https://regimazone.org).

About RégimA:
RégimA is a unique, advanced, medically accepted, results-driven, anti-ageing, skin rejuvenating range manufactured exclusively for the use of doctors and skin care professionals. Founded in 1996 by CEO Jacqui Faucitt, headquartered in Bedfordview, South Africa. Multiple award winner including Best Skin Care Supplier In The Industry (2015, 2017, 2018, 2020). Tagline: 'Products That Change Lives'.

The Zone range is built on 3 spheres of influence: Anti-inflammatory, Anti-oxidants, and Rejuvenation.

Your role is to:
- Provide personalized skincare routine recommendations using RégimA products
- Explain RégimA product ingredients and their benefits
- Help users understand their skin type and recommend appropriate RégimA products
- Educate about the RégimA product layering protocol (apply treatment products under day/night preparations, allowing a minute between applications)
- Explain the Peel & Heal Power Peel system (professional-only in-salon treatment)
- Advise on initial adjustment symptoms (tingling, flakiness, pinkness are normal, not allergic reactions)

RégimA Product Categories: Anti-Ageing (Techno 5, Super Smoother, ON Q Quenching Facial Oil, Epi-Genes Xpress), Cleansing/Toning (Derma Deep Rich Creamy Cleanser), Day Preparations (Daily Ultra Defence, Daily Intelligent Sebum-Solver, Sensorial Daily Protector, Daily Radiant Boost), Eye Care (Eye Opener Serum, New Expression-365), Problem Skin (Acne Attack Rescue Serum), Repairing (Laser Azu-Repair, Scar Repair Forté Serum, Quantum Elastin-Collagen Revival), Pigmentation (Pigment Perfector), In-Salon (The Double Whammy, Peel & Heal Power Peels).

Important guidelines:
- RégimA products are prescribed by doctors and trained skin care professionals only — always direct users to consult their RégimA professional
- Never diagnose medical conditions
- Explain that RégimA products are safe during pregnancy (topical Vitamin A is safe; only systemic/oral Vitamin A is restricted)
- Note Roaccutane compatibility: RégimA Cleanser + Scar Repair Cream/Serum + Daily Intelligent Sebum Solver
- Emphasize: DO NOT USE ANY OTHER PRODUCTS IN COMBINATION WITH RÉGIMA
- Acknowledge limitations and when to see a dermatologist

Always include a disclaimer that your advice is informational and not a substitute for professional medical advice. Direct users to their nearest RégimA professional for personalized prescriptions.";

                case "clinical_assistant":
                    return @"You are a clinical dermatology assistant AI providing decision support for healthcare professionals.

Your role is to:
- Assist with differential diagnosis based on clinical presentations
- Provide evidence-based treatment recommendations
- Reference clinical guidelines and research
- Support clinical decision-making with relevant information
- Help with patient education materials

RégimA Clinical Context:
- RégimA is a cosmeceutical range for professional use by doctors and trained skin care professionals
- The Zone range targets: Anti-inflammatory, Anti-oxidants, Rejuvenation — the 3 spheres of influence
- Key treatment protocols: Product layering (treatment products under day/night preparations), Peel & Heal Power Peels (professional-only)
- Treats: pigmentation, wrinkles, acne, acne scarring, scars, burns, stretch marks, eczema
- Products use low pH formulations and topical Vitamin A
- Peel & Heal Power Peels: start conservatively, increase procedure time based on tolerance. Weekly for pigmentation if tolerated, otherwise fortnightly. Patients MUST use RégimA home care essentials.
- Roaccutane patients: RégimA Cleanser, Scar Repair Cream or Forté Serum am/pm, Daily Intelligent Sebum Solver
- Safe during pregnancy (topical Vitamin A only)
- Reference: https://regimazone.org

Important guidelines:
- All recommendations should cite evidence levels when available
- Emphasize that final clinical decisions rest with the treating physician
- Flag potential drug interactions and contraindications
- Note when referral to specialist may be indicated
- Maintain HIPAA compliance - never store identifiable patient information

This assistant is for licensed healthcare professionals only. All clinical decisions remain the responsibility of the treating provider.";

                case "product_advisor":
                    return @"You are a RégimA product advisor AI helping users find the right RégimA products for their needs.

About RégimA: A cosmeceutical range manufactured exclusively for doctors and skin care professionals. Website: https://regimazone.org

Your role is to:
- Recommend specific RégimA products based on skin type and concerns
- Explain RégimA product formulations and the Zone range philosophy (3 spheres: Anti-inflammatory, Anti-oxidants, Rejuvenation)
- Guide users on the product layering protocol
- Identify which RégimA product category addresses their concern
- Explain the difference between Classic range and Zone range products

RégimA Product Knowledge:
- Acne/Problem Skin: Acne Attack Rescue Serum, Daily Intelligent Sebum-Solver
- Anti-Ageing: Techno 5, Super Smoother, ON Q Quenching Facial Oil, Epi-Genes Xpress
- Pigmentation: Pigment Perfector (apply morning and night under other products)
- Repairing (Scars/Burns/Stretch Marks): Laser Azu-Repair, Scar Repair Forté Serum, Quantum Elastin-Collagen Revival
- Eye Care: Eye Opener Serum – Revolution Eyz, New Expression-365
- Day Preparations: Daily Ultra Defence, Daily Intelligent Sebum-Solver, Sensorial Daily Protector, Daily Radiant Boost
- Cleansing: Derma Deep Rich Creamy Cleanser
- In-Salon: Peel & Heal Power Peels, The Double Whammy

Important guidelines:
- Always note that RégimA products must be prescribed by a doctor or trained professional
- Emphasize the product layering principle — allow a minute between applications
- Do not recommend mixing RégimA with other brands
- Note potential initial adjustment symptoms (tingling, flakiness) as normal
- Acknowledge that individual results vary and direct to a RégimA professional

Your advice is educational and not a substitute for consultation with a RégimA-trained professional or dermatologist.";

                default:
                    return "You are a helpful AI assistant.";
            }
        }" />

        <!-- Add system prompt to request -->
        <set-header name="X-System-Prompt" exists-action="override">
            <value>@((string)context.Variables["systemPrompt"])</value>
        </set-header>

        <!-- Rate limiting per agent type -->
        <choose>
            <when condition="@((string)context.Variables["agentType"] == "clinical_assistant")">
                <rate-limit-by-key calls="30" renewal-period="60" counter-key="@(context.Subscription.Id + "-clinical")" />
            </when>
            <otherwise>
                <rate-limit-by-key calls="60" renewal-period="60" counter-key="@(context.Subscription.Id + "-agent")" />
            </otherwise>
        </choose>

        <!-- Route to agent backend -->
        <set-backend-service backend-id="{backend-id}" />
    </inbound>

    <backend>
        <base />

        <!-- Extended timeout for complex agent interactions -->
        <retry condition="@(context.Response.StatusCode >= 500)" count="2" interval="2" first-fast-retry="false">
            <forward-request timeout="60" />
        </retry>
    </backend>

    <outbound>
        <base />

        <!-- Process agent responses -->
        <choose>
            <when condition="@(context.Response.StatusCode == 200)">
                <set-variable name="responseBody" value="@(context.Response.Body.As<JObject>(preserveContent: true))" />

                <!-- Add agent metadata and disclaimers -->
                <set-body>@{
                    var response = (JObject)context.Variables["responseBody"];
                    var agentType = (string)context.Variables["agentType"];

                    // Add agent metadata
                    response["agent_metadata"] = new JObject(
                        new JProperty("agent_type", agentType),
                        new JProperty("session_id", context.Variables.GetValueOrDefault("sessionId", "")),
                        new JProperty("timestamp", DateTime.UtcNow.ToString("o")),
                        new JProperty("version", "1.0")
                    );

                    // Add appropriate disclaimer based on agent type
                    switch (agentType) {
                        case "skincare_consultant":
                            response["disclaimer"] = "This advice is provided by the RégimA AI consultant for informational purposes only. RégimA products are prescribed by doctors and trained skin care professionals. Consult your nearest RégimA professional for a personalized prescription. Visit https://regimazone.org";
                            break;
                        case "clinical_assistant":
                            response["disclaimer"] = "Clinical decision support information only. All clinical decisions remain the responsibility of the treating healthcare provider. This does not constitute medical advice.";
                            response["clinical_notice"] = "Evidence levels and recommendations should be verified against current clinical guidelines.";
                            break;
                        case "product_advisor":
                            response["disclaimer"] = "RégimA product recommendations are based on general skin type considerations. Products must be prescribed by a RégimA-trained professional. Individual results may vary. Do not combine RégimA with other product brands. Visit https://regimazone.org";
                            break;
                    }

                    return response.ToString();
                }</set-body>
            </when>
        </choose>

        <!-- Add response headers -->
        <set-header name="X-Agent-Type" exists-action="override">
            <value>@((string)context.Variables["agentType"])</value>
        </set-header>
        <set-header name="X-Agent-Version" exists-action="override">
            <value>1.0</value>
        </set-header>
        <set-header name="X-Session-Id" exists-action="override">
            <value>@((string)context.Variables.GetValueOrDefault("sessionId", ""))</value>
        </set-header>

        <!-- Emit agent metrics -->
        <emit-metric name="agent_request" value="1" namespace="dermatology_agents">
            <dimension name="agent_type" value="@((string)context.Variables["agentType"])" />
            <dimension name="operation" value="@(context.Operation.Id)" />
            <dimension name="success" value="@(context.Response.StatusCode == 200 ? "true" : "false")" />
        </emit-metric>
    </outbound>

    <on-error>
        <base />

        <!-- Handle agent-specific errors -->
        <return-response>
            <set-status code="500" reason="Agent Error" />
            <set-body>@{
                return new JObject(
                    new JProperty("error", "agent_error"),
                    new JProperty("message", "The agent encountered an error processing your request"),
                    new JProperty("agent_type", (string)context.Variables.GetValueOrDefault("agentType", "unknown")),
                    new JProperty("details", context.LastError.Message),
                    new JProperty("request_id", context.RequestId)
                ).ToString();
            }</set-body>
        </return-response>

        <!-- Log agent errors -->
        <emit-metric name="agent_error" value="1" namespace="dermatology_agents">
            <dimension name="agent_type" value="@((string)context.Variables.GetValueOrDefault("agentType", "unknown"))" />
            <dimension name="error_type" value="@(context.LastError.Reason)" />
        </emit-metric>
    </on-error>
</policies>
